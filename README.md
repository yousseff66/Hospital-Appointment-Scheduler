# Hospital-Appointment-Scheduler
A full‑stack FastAPI + React app for hospital appointment scheduling with ML‑based waiting time prediction. Uses a Random Forest model, CSV persistence, smart ±2h time‑slot suggestions, and Dockerized backend for easy deployment.
